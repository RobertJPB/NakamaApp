const query = `query { Media(search: "Orb: On the Movements of the Earth", type: ANIME) { characters(perPage: 8, sort: ROLE) { edges { node { id name { full } image { large } } } } } }`; fetch('https://graphql.anilist.co', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' }, body: JSON.stringify({ query }) }).then(r=>r.json()).then(d=>console.log(JSON.stringify(d.data.Media.characters.edges.map(e=>({id: e.node.id.toString(), nombre: e.node.name.full, imagenUrl: e.node.image.large})), null, 2))).catch(console.error);
