services:
  - type: web
    name: nakama-api
    runtime: node
    rootDir: api
    buildCommand: npm install -g pnpm && pnpm install && pnpm prisma generate && pnpm build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        sync: false   # Lo pegas manualmente en el dashboard de Render
      - key: FRONTEND_URL
        sync: false   # La URL de Vercel, ej: https://nakama.vercel.app
      - key: CORS_ORIGINS
        sync: false   # Orígenes extra permitidos separados por coma (opcional)
      - key: REDIS_URL
        sync: false   # Opcional: activa caché compartida entre instancias
      - key: RUN_CRON
        value: "false"   # El cron de noticias corre en un worker aparte
      - key: LOG_LEVEL
        value: info
    healthCheckPath: /health
    plan: free
